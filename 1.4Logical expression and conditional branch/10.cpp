/*给出一名学生的语文和数学成绩，判断他是否恰好有一门课不及格（成绩小于60分）。
输入：包含两个在0到100之间的整数，分别是该生的语文成绩和数学成绩。
输出：若该生恰好有一门课不及格，输出1；否则输出0。*/
#include<stdio.h>
int main()
{	
	int x, y;
	scanf("%d%d", &x, &y);
	if ((x >= 60 && y < 60) || (x < 60 && y >= 60))	printf("1");
	else
	{
		printf("0");
	}
	return 0;
}
